import React from 'react';
import NavBar from "../../components/NavBar/NavBar";

const Teachers = () => {
    return (
        <div>
            <NavBar/>
            Teachers
        </div>
    );
};

export default Teachers;